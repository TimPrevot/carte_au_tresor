export default class Mountain {
    horizontalPos: number;
    verticalPos: number;

    constructor(hori: number, vert: number) {
        this.horizontalPos = hori;
        this.verticalPos = vert;
    }
}